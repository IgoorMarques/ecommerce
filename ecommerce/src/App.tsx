import { SafeAreaView, Text} from "react-native";

const App = ({ x }:any) =>{
    return(
        <SafeAreaView>
            <Text>Testando</Text>
        </SafeAreaView>
    )
};

export default App;